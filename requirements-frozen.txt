# Frozen environment used to produce the figures in the paper.
# Generated from the venv that ran the analyses; commit alongside
# requirements.txt as a reproducibility lock.
#
# To recreate the exact environment:
#   python3 -m venv venv
#   venv/bin/pip install -r requirements-frozen.txt
#
# requirements.txt holds the loose minimum-version spec for normal use;
# this file is the strict lock that produced the paper's PDFs.

# Direct deps
numpy==2.4.3
matplotlib==3.10.8
scipy==1.17.1

# Transitive deps (matplotlib backend stack)
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
packaging==26.0
pillow==12.1.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
